# ループエンジニアリング環境

AI が自走してアプリを実装するための環境。**AI は自分で検証できる範囲でしか自走できない**という前提で設計されている。

## スタック

Vite + React 19 + TypeScript / Vitest + Testing Library / Playwright / ESLint + Prettier

## 検証コマンド（唯一の合否オラクル）

| コマンド               | 内容                               | 用途                        |
| ---------------------- | ---------------------------------- | --------------------------- |
| `npm run verify:quick` | typecheck → lint → unit test       | 実装の途中で何度でも回す    |
| `npm run verify`       | quick + format:check → build → e2e | **周回の締めに必ず1回**     |
| `npm run format`       | Prettier で自動整形                | format:check が落ちたら実行 |

**完了判定は「`npm run verify` が緑か」だけ。** 自分の主観で「できたと思う」は完了ではない。

## ファイルの役割

| ファイル           | 役割                                         |
| ------------------ | -------------------------------------------- |
| `docs/SPEC.md`     | **何を作るか**。人間が書く仕様。唯一の情報源 |
| `.loop/backlog.md` | **どの順で作るか**。タスクキュー             |
| `.loop/state.md`   | **今どこか**。毎周回で上書き                 |
| `.loop/journal.md` | **何をやったか**。追記専用                   |

コンテキストが圧縮されても、この4ファイルを読めば文脈を復元できる。**自分の記憶ではなくファイルを信頼すること。**

## 周回の手順（厳守）

1. `.loop/state.md` と `.loop/backlog.md` を読む
2. Todo から未完了タスクを **1つだけ** 選び、In Progress に移す
3. 実装する（途中で `npm run verify:quick` を何度回してもよい）
4. `npm run verify` を回す
5. **緑なら** → コミット → backlog の該当タスクを Done へ（commit hash 付き）→ state.md 上書き → journal.md 追記
6. **赤なら** → 原因を直して 4 に戻る。連続失敗回数を state.md に記録する

### UI を変更した周回では、必ず目で確認する

`npm run verify` の後、`.loop/screenshots/home.png` を **Read ツールで画像として開いて確認する**。

テストが緑でもレイアウトは壊れうる。`toBeVisible()` は、要素が画面外に飛んでいても、背景と同化していても、カラムが潰れていても通る。**この目視だけが、その崩れを捕まえられる。**

新しい画面を作ったら、その画面のスクリーンショットも `.loop/screenshots/<画面名>.png` の固定パスで撮るテストを追加すること。

### コミットメッセージ

```
T-003 タスクを完了/未完了にトグルできる
```

タスク ID を先頭に置く。journal から追跡できるようにするため。

## 禁止事項

- ❌ **テストを緩める / skip する / 削除して「通った」ことにする**
  → 実装を直すこと。テストを変更してよいのは SPEC の受け入れ条件が変わった時だけで、その場合は理由を journal.md に書く
- ❌ `npm run verify` が赤のままコミットする
- ❌ 1周回で2つ以上のタスクに手を出す
- ❌ **SPEC に無い機能を追加する**（「あったほうが良さそう」で機能を足さない）
- ❌ `git push` / `git reset --hard` / `git clean` / ファイルの一括削除
- ❌ `docs/SPEC.md` を AI が書き換える（仕様は人間のもの。疑問があれば止まって聞く）
- ❌ 依存パッケージの追加（SPEC の技術制約で許可されている場合を除く）

## 停止条件

以下に該当したら**自走をやめて、状況を報告する**。

- 同じタスクで `npm run verify` が **3回連続で失敗** した
  → journal.md に失敗内容と試したことを書いてから停止
- backlog の Todo が空になった
- SPEC の解釈に迷った / SPEC に書かれていない判断を迫られた
  → **推測で実装せず** 停止して質問する
- 停止条件を回避するために SPEC やテストを書き換えたくなった
  → それは停止すべきサイン。書き換えずに停止する

## 報告は短く

各周回の報告は **1〜3行**。「何をしたか / verify 結果 / 次の一手」だけ。
周回ごとに長文を出すとコンテキストを食い潰し、自走できる周回数が減る。詳細は journal.md に書けばよい。

## 環境上の注意

- Windows 環境。シェルは PowerShell が主
- Playwright の dev サーバーは `webServer` 設定が自動で起動・停止する。**手動でサーバーを起動しないこと**（ゾンビプロセスとポート衝突の原因になる）
- E2E を単体で回すときは `npm run e2e`（`dist/` のビルドを含む）
