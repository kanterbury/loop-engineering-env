# Backlog

> `docs/SPEC.md` を作業単位に分解したタスクキュー。`/loop-plan` が生成し、`/loop-iterate` が上から消化する。
>
> ルール:
>
> - 1タスク = 1周回 = 1コミット で終わる粒度にする（「1コンポーネント」「1機能の1振る舞い」程度）
> - 各タスクに **検証可能な受け入れ条件** と **検証方法** を必ず書く
> - ID (`T-001`) を振る。journal とコミットメッセージから参照する
> - 実装中に気づいた必要作業（不足テスト、リファクタ）は Todo に追記してよい
> - **SPEC に無い機能は追加しない**

## In Progress

（同時に1つだけ。空でもよい）

## Todo

### データ層

- [ ] T-001 タグ定義と出題タグプールを作る
  - 受け入れ条件: `src/data/tags.ts` にタグが分類（資格/職業/位置/特性）付きで定義され、総数が 29 個である（資格3・職業8・位置2・特性16）
  - 受け入れ条件: `Tag` 型がタグ名のユニオン型で、定義外の文字列を代入すると型エラーになる
  - 受け入れ条件: 出題タグプールがタグ全体とは別の配列として定義されている
  - 受け入れ条件: 出題タグプールの全要素が定義済みタグに含まれる
  - 検証: Vitest (`src/data/tags.test.ts`) — 分類ごとの件数・総数・プールの妥当性

- [ ] T-002 確定枠テーブルを作る
  - 受け入れ条件: `src/data/guaranteed.ts` に 10 行以上の確定枠テーブルがある
  - 受け入れ条件: 各行が `{ タグ配列(1〜3個), 最低レアリティ(4|5|6), 対象オペレーター名の配列 }` を持つ
  - 受け入れ条件: 全ての行のタグが出題タグプールに含まれる
  - 受け入れ条件: 他の行の部分集合となる冗長な行が存在しない（最小性）
    - 判定は完全一致で行うため、**冗長な行があると本来不正解の選択が正解になる**。表示上の都合ではなく正しさの制約
  - 受け入れ条件: `{上級エリート}` と `{エリート}` の行が存在する
  - 受け入れ条件: ファイル先頭に、出典・検証状況・不変条件を記したコメントがある
  - 検証: Vitest (`src/data/guaranteed.test.ts`) — 行数・タグがプール内か・最小性・必須行の存在
  - 注記: **データ内容の正しさは AI が保証できない。** 構造的な制約のみをテストし、正確性は H-1 で人間が検証する

### 判定ロジック（UI 非依存の純粋関数）

- [ ] T-003 確定枠の判定関数を実装する
  - 受け入れ条件: タグ集合とテーブルを渡すと `{ isGuaranteed, matchedEntry }` を返す
  - 受け入れ条件: タグ集合と完全一致する行があれば確定枠と判定される
  - 受け入れ条件: タグの順序が異なっていても、集合として一致すれば確定枠と判定される
  - 受け入れ条件: **行のタグを部分集合として含むだけの選択は確定枠ではない**（`{重装,狙撃}` の行があっても `{重装,狙撃,火力}` は不正解）
  - 受け入れ条件: 行のタグの一部しか選んでいない選択は確定枠ではない（`{重装,狙撃}` の行に対し `{重装}` は不正解）
  - 受け入れ条件: タグを 0 個または 4 個以上渡すと例外を投げる
  - 検証: Vitest (`src/logic/evaluate.test.ts`) — **テスト用の固定テーブル**に対して書く（実データ差し替えでテストが壊れないようにするため）

- [ ] T-004 5タグから確定枠を全列挙する関数を実装する
  - 受け入れ条件: 提示5タグの部分集合となる行を全て返す
  - 受け入れ条件: 該当する行が無い5タグに対して空配列を返す
  - 受け入れ条件: 結果がタグ数の少ない順に並ぶ
  - 検証: Vitest (`src/logic/enumerate.test.ts`) — 固定テーブル

### 出題

- [ ] T-005 シード付き乱数を実装する
  - 受け入れ条件: 同じシードから作った乱数列が常に同じである
  - 受け入れ条件: 配列から重複なく n 個を抽選するヘルパーがある
  - 受け入れ条件: 重み付き抽選ができる（特定タグの出現率を下げるため）
  - 検証: Vitest (`src/logic/random.test.ts`)

- [ ] T-006 出題生成関数を実装する（テーブル逆引き方式）
  - 受け入れ条件: 生成される5タグが重複せず、全て出題タグプールに含まれる
  - 受け入れ条件: 「確定枠あり」を要求すると、テーブルから1行選びそのタグを含む5タグが返る
  - 受け入れ条件: 「確定枠なし」を要求すると、確定枠を1つも含まない5タグが返る
  - 受け入れ条件: 同じシードなら常に同じ問題が生成される
  - 受け入れ条件: `上級エリート` `エリート` の出現率が他タグより低い
  - 検証: Vitest (`src/logic/question.test.ts`) — 固定テーブル + 固定シード

### 画面

- [ ] T-007 クイズ画面の骨格とタグ表示を作る
  - 受け入れ条件: 5つのタグがボタンとして表示される
  - 受け入れ条件: タグをクリックすると選択状態になり、もう一度クリックで解除される
  - **注記: 環境構築時のプレースホルダ画面（`Loop Engineering Env` の見出し）とその `src/App.test.tsx` はここで置き換える。SPEC で画面仕様が確定したための正当な差し替えであり、「テストを緩める」には当たらない。journal に置き換えた旨を記録すること。**
  - 検証: Vitest (`src/App.test.tsx`) + `.loop/screenshots/home.png` を Read で目視

- [ ] T-008 選択の上限と回答ボタンの活性制御を実装する
  - 受け入れ条件: 3つ選択済みで4つ目をクリックしても選択されない
  - 受け入れ条件: タグを1つも選んでいないと「回答する」ボタンが押せない
  - 受け入れ条件: 「確定枠なし」ボタンが常に押せる状態で表示される
  - 検証: Vitest (`src/App.test.tsx`)

- [ ] T-009 正誤判定と結果メッセージを実装する
  - 受け入れ条件: 確定枠と完全一致するタグを選んで回答すると「正解」と表示される
  - 受け入れ条件: 確定枠でないタグを選んで回答すると「不正解」と表示される
  - 受け入れ条件: 確定枠に余分なタグを足した選択は「不正解」と表示される
  - 受け入れ条件: 確定枠が複数ある問題で、そのうち1つと完全一致すれば正解になる
  - 受け入れ条件: 確定枠が無い問題で「確定枠なし」を押すと正解になる
  - 受け入れ条件: 確定枠がある問題で「確定枠なし」を押すと不正解になる
  - 検証: Vitest (`src/App.test.tsx`) — 固定シードで問題を再現して検証

- [ ] T-010 結果画面に確定枠の一覧を表示する
  - 受け入れ条件: 正解・不正解にかかわらず、その問題の確定枠が全て表示される
  - 受け入れ条件: 各確定枠にタグの組み合わせ・最低レアリティ・対象オペレーター名が表示される
  - 受け入れ条件: 確定枠が無い問題では、確定枠が無い旨が表示される
  - 検証: Vitest (`src/App.test.tsx`) + `.loop/screenshots/result.png` を撮って Read で目視

- [ ] T-011 「次の問題へ」で出題を更新する
  - 受け入れ条件: 「次の問題へ」を押すと新しい5タグが表示される
  - 受け入れ条件: 押した後、タグの選択状態と結果表示がリセットされる
  - 検証: Vitest (`src/App.test.tsx`)

### スコア

- [ ] T-012 連続正解数と正答率を表示する
  - 受け入れ条件: 正解すると連続正解数が 1 増える
  - 受け入れ条件: 不正解すると連続正解数が 0 に戻る
  - 受け入れ条件: 正答率が「正解数 / 回答数」で表示される
  - 受け入れ条件: 未回答時の正答率表示が壊れない（0 除算しない）
  - 検証: Vitest (`src/App.test.tsx`)

- [ ] T-013 スコアを localStorage に永続化する
  - 受け入れ条件: 回答するとスコアが localStorage に保存される
  - 受け入れ条件: 再マウント時に保存済みスコアが復元される
  - 受け入れ条件: localStorage が空／壊れた値のとき初期値で起動しクラッシュしない
  - 検証: Vitest (`src/logic/score.test.ts` または `src/App.test.tsx`)

- [ ] T-014 スコアのリセットボタンを実装する
  - 受け入れ条件: リセットボタンを押すと連続正解数と正答率が 0 になる
  - 受け入れ条件: リセット後、localStorage の値も初期化される
  - 検証: Vitest (`src/App.test.tsx`)

### 仕上げ

- [ ] T-015 E2E で一連の操作を通す
  - 受け入れ条件: タグを選ぶ→回答する→結果が出る→次の問題へ、が E2E で通る
  - 受け入れ条件: `.loop/screenshots/home.png` と `.loop/screenshots/result.png` が生成される
  - 受け入れ条件: リロード後もスコアが保持されることを E2E で確認する
  - 検証: Playwright (`e2e/quiz.spec.ts`)

## Done

- [x] T-000 環境構築 — verify パイプラインがグリーンな基準点 (commit 99426e7)

## 人間のタスク（AI は手を出さない）

- [ ] H-1 確定枠テーブルと出題タグプールの正確性を検証する
  - T-002 完了後に実施。AI が作成した**未検証データ**のため、誤りが含まれる前提でレビューする
  - 特に確認すべきは **網羅性の不変条件**: 出題タグプール内のタグから作れる 1〜3 個の全組み合わせについて、確定枠であるものが漏れなくテーブルに存在するか
  - 漏れがあると「本当は確定枠があるのに『なし』が正解」と教えることになる。**機械的には検証できないため、人間のレビューが唯一の担保**
  - プールを広げるときは、広げた範囲の網羅性も同時に確認する
  - 修正は `src/data/` 配下のみで完結する設計になっており、ロジックとテストには波及しない
