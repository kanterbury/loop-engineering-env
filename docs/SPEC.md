# SPEC — 作るアプリの仕様

> このファイルが「何を作るか」の唯一の情報源です。
> AI はここに書かれていないものを作りません。逆に、ここに書けば作ります。
>
> **書き方の基準: 受け入れ条件が、そのまま Vitest / Playwright のテストに翻訳できるか。**
> 「使いやすいこと」は翻訳できません。「送信ボタンを押すと一覧の先頭に追加される」は翻訳できます。
>
> 以下はテンプレートです。`<!-- ここから -->` 以降を書き換えて使ってください。

<!-- ここから -->

## 目的

（このアプリは何のためのものか。1〜3行）

例: ブラウザだけで完結する個人用の TODO 管理。ログイン不要、データはローカルに保持する。

## 対象ユーザー

（誰が使うか。1〜2行）

## 機能

各機能に **検証可能な受け入れ条件** を必ず付けること。

### F-1 （機能名）

（1〜2行の説明）

受け入れ条件:

- （〜が表示される / 〜をクリックすると〜になる、の形で書く）
- （複数書いてよい。それぞれがテスト1本になる）

### F-2 （機能名）

受け入れ条件:

-

## 非目標（スコープ外）

**この節を省略しないこと。** 書かないと AI が「あったほうがよさそう」な機能を勝手に足し始めます。

- 例: ユーザー認証は作らない
- 例: サーバーサイド / API は作らない
- 例: 多言語対応はしない

## 技術制約

- Vite + React + TypeScript（変更しない）
- 状態管理ライブラリは追加しない（React の標準機能で足りる範囲で作る）
- 永続化は localStorage のみ
- （その他、AI に守らせたい制約をここに書く）

## 画面

（画面が複数ある場合のみ。ルーティングと各画面の役割を列挙）

- `/` — （役割）
